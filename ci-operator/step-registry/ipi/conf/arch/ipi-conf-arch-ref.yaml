ref:
  as: ipi-conf-arch
  from_image:
   namespace: origin
   name: ubi
   tag: '8'
  commands: ipi-conf-arch-commands.sh
  env:
  - name: ARCH
#  - name: MULTIARCH_RELEASE_IMAGE_LATEST 
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    The IPI ARCH configure step generates the ARCH-specific install-config.yaml contents based on the cluster profile and optional input files.

