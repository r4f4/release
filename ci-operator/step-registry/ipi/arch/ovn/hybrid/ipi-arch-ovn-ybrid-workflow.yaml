workflow:
  as: ipi-arch-ovn-hybrid
  steps:
    pre:
    - chain: ipi-conf-arch
    - ref: ovn-conf
    - ref: ovn-conf-hybrid-manifest
    - chain: ipi-install
    post:
    - chain: ipi-deprovision
  documentation: |-
    Openshift cluster configured with Hybrid OVN network
