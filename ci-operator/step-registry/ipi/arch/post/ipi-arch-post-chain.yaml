chain:
  as: ipi-arch-post
  steps:
#  - ref: gather-must-gather
  - chain: ipi-deprovision
  documentation: |-
    The IPI cleanup step contains all steps that gather and deprovision an Openshift cluster on multi-arch, provisioned by the 'ipi-arch-pre' chain.
